# literate-dollop
Keep the current Capacitor shell for Build 14 and resubmission, then migrate to Expo/React Native in a separate repo with shared TypeScript contracts. Use SwiftUI only for the existing VetTrackControl widgets/extension via native integration, not as a wholesale app foundation.
